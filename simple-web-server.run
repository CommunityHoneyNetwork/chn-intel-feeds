#!/bin/bash

trap "exit 130" SIGINT
trap "exit 137" SIGKILL
trap "exit 143" SIGTERM

if [[ ${DEBUG} == "true" ]]
then
  set -o xtrace
  DBG="-d"
fi

mkdir -p /var/www/feeds
cd /var/www/feeds
exec python3 -m http.server ${PORT:-9000}
