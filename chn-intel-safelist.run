#!/bin/bash

trap "exit 130" SIGINT
trap "exit 137" SIGKILL
trap "exit 143" SIGTERM

if [[ ${DEBUG} == "true" ]]
then
  set -o xtrace
  DBG="-d"
fi

mkdir -p /var/www/safelists
cd /var/www/safelists
exec python3 /opt/feed_safelister.py -s ${SAFELIST_SLEEP:-60} -r ${DBG}

